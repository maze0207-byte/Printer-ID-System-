# ID Forge Desktop App

## نظام إدارة بطاقات الهوية - جامعة ساكسوني مصر

تطبيق سطح مكتب متكامل لإدارة وطباعة بطاقات الهوية للطلاب والموظفين.

### ✨ المميزات

#### 🏛️ الهيكل التنظيمي
- إدارة الكليات والأقسام والبرامج
- دعم ثنائي اللغة (English/العربية)

#### 👥 إدارة الأشخاص
- الطلاب، الموظفين، الزوار
- توليد رقم جامعي تلقائي
- البيانات الشخصية والصور

#### 🎴 إدارة البطاقات
- تصميم البطاقة مع معاينة مباشرة
- رمز QR للتحقق
- الاستيراد الجماعي من CSV
- **أبعاد مخصصة للبطاقة**
- **طباعة جماعية**

#### 🖨️ الطباعة
- طباعة مباشرة بدون نافذة المتصفح
- دعم أحجام متعددة (CR80, CR79, مخصص)
- طباعة دفعات كبيرة مع شريط تقدم

#### 🔐 الأمان
- صلاحيات المستخدمين (مدير، مشغل، مشاهد)
- سجل التدقيق الكامل
- النسخ الاحتياطي والاستعادة

#### 📊 التقارير
- لوحة تحكم بإحصائيات حية
- تصدير PDF و Excel

---

### 🚀 التشغيل

#### المتطلبات
- Node.js 18+ (يُفضل Node.js 20 LTS)
- npm أو yarn

> **ملاحظة:** يستخدم التطبيق `sql.js` (SQLite بالـ JavaScript) لذلك **لا تحتاج** Python أو Visual Studio Build Tools!

#### تثبيت وتشغيل
```bash
cd electron-app
npm install
npm run dev
```

#### بناء التطبيق
```bash
# لنظام Windows
npm run package:win

# لنظام Mac
npm run package:mac

# لنظام Linux
npm run package:linux
```

---

### 📁 هيكل الملفات

```
electron-app/
├── src/
│   ├── main.ts          # العملية الرئيسية لـ Electron
│   └── preload.ts       # جسر التواصل مع الواجهة
├── resources/
│   ├── icon.ico         # أيقونة Windows
│   ├── icon.icns        # أيقونة Mac
│   └── icon.png         # أيقونة Linux
├── dist/                # ملفات JavaScript المترجمة
├── release/             # التطبيق المعبأ
├── package.json
├── tsconfig.json
└── DATABASE_ERD.md      # رسم قاعدة البيانات
```

---

### 💾 قاعدة البيانات

يستخدم التطبيق **SQLite** كقاعدة بيانات محلية.

موقع قاعدة البيانات:
- Windows: `%APPDATA%/id-forge-desktop/id-forge.db`
- Mac: `~/Library/Application Support/id-forge-desktop/id-forge.db`
- Linux: `~/.config/id-forge-desktop/id-forge.db`

---

### 🔄 النسخ الاحتياطي

- **تصدير**: يحفظ نسخة من قاعدة البيانات كملف `.db`
- **استيراد**: يستعيد قاعدة البيانات من نسخة احتياطية

---

### 📐 أحجام البطاقات

| النوع | العرض | الارتفاع |
|-------|-------|---------|
| CR80 (قياسي) | 85.6 مم | 53.98 مم |
| CR79 | 83.9 مم | 51.0 مم |
| CR100 | 97.0 مم | 67.0 مم |
| مخصص | حسب الطلب | حسب الطلب |

---

### 👥 صلاحيات المستخدمين

| الدور | الصلاحيات |
|-------|----------|
| admin (مدير) | كل الصلاحيات |
| operator (مشغل) | إضافة وتعديل وطباعة |
| viewer (مشاهد) | عرض فقط |
